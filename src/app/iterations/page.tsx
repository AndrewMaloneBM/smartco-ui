import { IterationExplorer } from "@/components/iterations/iteration-explorer";

export const metadata = {
  title: "SmartCo — Iteration explorer",
};

export default function IterationsPage() {
  return <IterationExplorer />;
}
